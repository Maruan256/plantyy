<div class="container mt-5">
  <%# <div class="row">
    <div class="col"> %>
      <h1 class="h2 my-3 d-flex justify-content-center">Garden inspirations🌿</h1>
        <div data-controller="search-garden-kits">
        <%= form_with url: garden_kits_path, method: :get, html: {class: 'mb-4', data: { search_garden_kits_target: 'form'} } do |f|%>
          <%= f.text_field :query,
                          class: 'form-control',
                          placeholder: 'I\'m looking for...',
                          value: params[:query],
                          data: { search_garden_kits_target: 'input', action: 'keyup->search-garden-kits#update'}
                          %>
          <% end %>
            <%= render 'list', garden_kits: @garden_kits %>
          </div>
        </div>
    <%# </div>
  </div> %>
</div>
